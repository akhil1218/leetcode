# Write your MySQL query statement below
select p.product_name,sum(unit) as unit from Products p join Orders o on p.product_id=o.product_id and date_format(order_date,'%Y-%m')='2020-02' group by p.product_id,product_name having sum(unit)>=100